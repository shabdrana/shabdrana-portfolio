const menu=document.querySelector('.menu');const navLinks=document.querySelector('.nav-links');if(menu){menu.addEventListener('click',()=>{navLinks.style.display=navLinks.style.display==='flex'?'none':'flex';navLinks.style.position='absolute';navLinks.style.top='72px';navLinks.style.left='0';navLinks.style.right='0';navLinks.style.padding='18px';navLinks.style.background='rgba(7,16,24,.98)';navLinks.style.flexDirection='column';navLinks.style.borderBottom='1px solid rgba(255,255,255,.09)'})}
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const modal=document.getElementById('certModal'),title=document.getElementById('modalTitle'),img=document.getElementById('modalImage'),pdf=document.getElementById('modalPdf');
function openModal(t,src,isPdf=false){title.textContent=t;if(isPdf){img.hidden=true;pdf.hidden=false;pdf.src=src}else{pdf.hidden=true;pdf.src='';img.hidden=false;img.src=src}modal.classList.add('show');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}
function closeModal(){modal.classList.remove('show');modal.setAttribute('aria-hidden','true');pdf.src='';img.src='';document.body.style.overflow=''}
document.querySelectorAll('.image-cert').forEach(card=>card.addEventListener('click',()=>openModal(card.dataset.title,card.dataset.img,false)));document.querySelectorAll('.open-pdf').forEach(btn=>btn.addEventListener('click',()=>openModal(btn.dataset.title,btn.dataset.pdf,true)));document.querySelector('.modal-close').addEventListener('click',closeModal);document.querySelector('.modal-backdrop').addEventListener('click',closeModal);document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});
